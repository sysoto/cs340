# CS340 Project

## Setup Instructions

Ensure the database exists in MariaDB before starting the application.

Once this project is on the class server, run the following commands:

```
npm install
npm run production
```

After the server starts, check the application URL and make sure it ends with:

```
:1213
```
